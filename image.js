const galleryItems=[...document.querySelectorAll('.gallery-item')],lightbox=document.getElementById('lightbox'),img=document.getElementById('lightbox-img'),title=document.getElementById('lightbox-title'),category=document.getElementById('lightbox-category');let visibleItems=[...galleryItems],currentIndex=0;function update(){const item=visibleItems[currentIndex];img.src=item.querySelector('img').src;img.alt=item.querySelector('img').alt;title.textContent=item.querySelector('h3').textContent;category.textContent=item.querySelector('p').textContent}function open(i){currentIndex=i;update();lightbox.classList.add('show');document.body.style.overflow='hidden'}function close(){lightbox.classList.remove('show');document.body.style.overflow='auto'}function next(){if(!visibleItems.length)return;currentIndex=(currentIndex+1)%visibleItems.length;update()}function prev(){if(!visibleItems.length)return;currentIndex=(currentIndex-1+visibleItems.length)%visibleItems.length;update()}galleryItems.forEach(item=>item.addEventListener('click',()=>open(visibleItems.indexOf(item))));document.getElementById('close').onclick=close;document.getElementById('next').onclick=next;document.getElementById('prev').onclick=prev;lightbox.addEventListener('click',e=>{if(e.target===lightbox)close()});document.addEventListener('keydown',e=>{if(!lightbox.classList.contains('show'))return;if(e.key==='Escape')close();if(e.key==='ArrowRight')next();if(e.key==='ArrowLeft')prev()});document.querySelectorAll('.filter-btn').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filter-btn').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;galleryItems.forEach(item=>item.classList.toggle('hidden',f!=='all'&&item.dataset.category!==f));visibleItems=galleryItems.filter(item=>!item.classList.contains('hidden'))}));
